@{
    ViewData["Title"] = "Index";
}
@model List<PlayerViewModel>

<h1>List of Player</h1>
<hr />

<div class="row justify-content-end">
    <div class="col-4 d-flex justify-content-end">
        @Html.ActionLink("Add", "Create", "Players", null, new { @class = "btn btn-primary" })
    </div>
</div>
<div class="row">
    <table class="table table-striped">
        <thead>
            <tr>
                <th scope="col">Id</th>
                <th scope="col">TeamId</th>
                <th scope="col">TeamName</th>
                <th scope="col">Number</th>
                <th scope="col">Fullname</th>
                <th scope="col">Operation</th>
            </tr>
        </thead>

        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <th scope="row">@item.Id</th>
                    <td>@item.TeamId</td>
                    <td>@item.TeamName</td>
                    <td>@item.Number</td>
                    <td>@item.FullName</td>
                    <td>
                        <a class="btn btn-warning" asp-controller="Teams" asp-action="Edit" asp-route-id="@item.Id">
                            <i class="far fa-pen-to-square"></i>

                        </a>
                        <a class="btn btn-danger" asp-controller="Teams" asp-action="Delete" asp-route-id="@item.Id">
                            <i class="fas fa-trash-can"></i>
                        </a>
                    </td>
                </tr>
            }


        </tbody>
    </table>
</div>




