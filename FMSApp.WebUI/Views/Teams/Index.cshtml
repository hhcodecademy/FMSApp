@{
    ViewData["Title"] = "Index";
}
@model List<Team>

<h1>List of Teams</h1>
<hr />

<div class="row justify-content-end">
    <div class="col-4 d-flex justify-content-end">
        @Html.ActionLink("Add", "Create", "Teams", null, new { @class = "btn btn-primary" })
    </div>
</div>
<div class="row">
    <table class="table table-striped">
        <thead>
            <tr>
                <th scope="col">Id</th>
                <th scope="col">Name</th>
                <th scope="col">Operation</th>
            </tr>
        </thead>

        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <th scope="row">@item.Id</th>
                    <td>@item.Name</td>
                    <td>
                        <a class="btn btn-warning" asp-controller="Teams" asp-action="Edit" asp-route-id="@item.Id">
                            <i class="far fa-pen-to-square"></i>

                        </a>
                        <a class="btn btn-danger" asp-controller="Teams" asp-action="Delete" asp-route-id="@item.Id">
                            <i class="fas fa-trash-can"></i>
                        </a>
                        <a class="btn btn-primary" asp-controller="Players" asp-action="Create" asp-route-teamId="@item.Id">
                            <i class="fas fa-plus"></i>
                        </a>
                        <a class="btn btn-success" asp-controller="Players" asp-action="Index" asp-route-teamId="@item.Id">
                            <i class="fas fa-list"></i>
                        </a>
                    </td>
                </tr>
            }


        </tbody>
    </table>
</div>


